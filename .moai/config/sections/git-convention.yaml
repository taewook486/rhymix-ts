git_convention:
    auto_detection:
        confidence_threshold: 0.5
        enabled: true
        fallback: conventional-commits
        sample_size: 100
    convention: auto
    validation:
        enforce_on_push: false
        max_length: 100
