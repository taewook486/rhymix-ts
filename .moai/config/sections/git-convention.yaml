git_convention:
    convention: auto
    auto_detection:
        enabled: true
        sample_size: 100
        confidence_threshold: 0.5
        fallback: conventional-commits
    validation:
        enforce_on_push: false
        max_length: 100
