{
  "name": "@rhymix-ts/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./install": "./src/install-validate.ts"
  },
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:push": "prisma db push",
    "prisma:migrate": "prisma migrate dev",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:studio": "prisma studio",
    "db:generate": "prisma generate",
    "lint": "echo 'no lint'",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf node_modules/.prisma .turbo"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "@rhymix-ts/core": "workspace:*",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.0",
    "prisma": "^6.0.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.9.0"
  },
  "prisma": {
    "schema": "prisma/schema.prisma"
  }
}
